import { AboutUs, MainInfo, WeOffer } from '@/widgets';

export default function AboutPage() {
  return (
    <div className={'w-full'}>
      <AboutUs />
      <WeOffer />
      <MainInfo />
    </div>
  );
}
